"""alpha-compass Python sidecar package.

Stateless fetch + AI service. Persistence lives in Rust (Tauri Core); this
service only fetches, normalizes, and summarizes, returning JSON.
"""

__version__ = "0.1.0"
